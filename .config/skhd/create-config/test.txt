alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east
shift + alt - m : yabai -m window --toggle zoom-fullscreen
shift + alt - b : yabai -m space --balance
shift + alt - h : yabai -m window --swap west
shift + alt - j : yabai -m window --swap south
shift + alt - k : yabai -m window --swap north
shift + alt - l : yabai -m window --swap east
ctrl + alt - h : yabai -m window --warp west
ctrl + alt - j : yabai -m window --warp south
ctrl + alt - k : yabai -m window --warp north
ctrl + alt - l : yabai -m window --warp east
ctrl + alt - 0 : yabai -m window --space 0 | index=0; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 1 : yabai -m window --space 1 | index=1; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 2 : yabai -m window --space 2 | index=2; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 3 : yabai -m window --space 3 | index=3; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 4 : yabai -m window --space 4 | index=4; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 5 : yabai -m window --space 5 | index=5; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 6 : yabai -m window --space 6 | index=6; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 7 : yabai -m window --space 7 | index=7; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 8 : yabai -m window --space 8 | index=8; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - 9 : yabai -m window --space 9 | index=9; eval "$(yabai -m query --spaces | jq --argjson index "${index}" -r '(.[] | select(.index == $index).windows[0]) as $wid | if $wid then "yabai -m window --focus "" + ($wid | tostring) + """ else "skhd --key "ctrl - " + (map(select(."is-native-fullscreen" == 0)) | index(map(select(.index == $index))) + 1 % 10 | tostring) + """ end')"
ctrl + alt - q : yabai --stop-service
ctrl + alt - s : yabai --start-service
ctrl + alt - r : yabai --restart-service